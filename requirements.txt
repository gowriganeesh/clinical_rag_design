# --- UI ---
streamlit>=1.36.0

# --- Core pipeline ---
azure-search-documents>=11.5.1
openai>=1.40.0
python-dotenv>=1.0.0
truststore>=0.10.0

# --- PHI scrubbing (optional but recommended) ---
# After installing, download a spaCy model:
#   python -m spacy download en_core_web_lg
# If Presidio or the model are unavailable, deid falls back to regex.
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
spacy>=3.7

# --- Evaluation ---
ragas>=0.2.0
deepeval>=1.0.0
langchain-openai>=0.2.0
datasets>=2.0.0
